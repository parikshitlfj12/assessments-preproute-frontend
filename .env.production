# Production build: call the same Vercel origin so the browser makes same-origin
# requests. The `/api/*` path is proxied to the Railway backend server-side by the
# rewrite in vercel.json, which sidesteps the backend's browser CORS allowlist.
VITE_API_BASE_URL=/api
